import styled from 'styled-components/native';
import {buttonStyle} from './stringStyles';

export const Container = styled.TouchableOpacity`
  ${buttonStyle};
  justify-content: center;
  align-items: center;
`;

export const Label = styled.Text``;
